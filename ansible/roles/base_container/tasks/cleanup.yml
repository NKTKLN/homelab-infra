- name: Autoremove unused packages (Debian)
  ansible.builtin.apt:
    autoremove: true
    autoclean: true
    purge: true
  when:
    - ansible_facts['os_family'] == 'Debian'
    - base_container_cleanup_autoremove | bool
